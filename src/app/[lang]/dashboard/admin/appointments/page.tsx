// Admin sees the same appointments management as staff (with shared full CRUD).
// The underlying page already declares "use client" internally.
export { default } from '@/app/[lang]/dashboard/staff/appointments/page';
